[build]
  publish = "."

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
  conditions = {role = ["admin"]} # This is just a placeholder example, we don't need complex redirects for this static app.

# We'll keep it simple: publish the current directory.
